{
  "name": "trucks",
  "module": "src/index.ts",
  "scripts": {
    "start": "node build/index.js",
    "dev": "concurrently \"node --watch build/index.js\" \"tsc --watch\"",
    "test": "playwright test",
    "build": "tsc"
  },
  "devDependencies": {
    "@playwright/test": "^1.40.0",
    "@types/node": "^20.10.0",
    "concurrently": "latest",
    "typescript": "latest"
  },
  "peerDependencies": {},
  "dependencies": {
    "graphql": "^16.8.1",
    "graphql-request": "^6.1.0",
    "playwright": "latest"
  }
}
